node_modules/
.env
*.log

# npm lockfile — the package ships as a library, consumers resolve their own
package-lock.json
.waSession 

# npm pack output — a build artifact of checking a release, never a source file
*.tgz
